export interface Product {
    id: number;
    name: string;
    price: number;
    imageUrl: string;
    description: string;
}

export interface Category {
    id: number;
    name: string;
    iconUrl: string;
}
